# Ruff 配置（仓库级）。
# 仅忽略 FastAPI / 离线容错处的惯用法告警；其余规则（F/E/W/I/B/C4/SIM 等）保持生效，
# 真实缺陷（如未定义名称、未使用导入）仍会被 CI 拦截。
[lint]
ignore = [
    "B008",  # FastAPI 路由签名用 Depends(...) 作默认值，属惯用法
    "S110",  # 离线埋点 / 容错处有意静默吞掉异常
    "BLE001", # 同上：有意捕获 Exception 做兜底
]
